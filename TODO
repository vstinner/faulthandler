 * tests.py: test register() with something else than SIGUSR1 on Windows?
 * fix tests on Windows (permission denied)
 * disable handlers at fork?
 * what about child processes?
 * use something else than Py_AtExit() to unload the module?

